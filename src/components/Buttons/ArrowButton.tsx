/** Re-export v2 ArrowButton — keep legacy import path working. */
export { default } from "@/components/v2/buttons/ArrowButton";
export type { ArrowButtonProps } from "@/components/v2/buttons/ArrowButton";
